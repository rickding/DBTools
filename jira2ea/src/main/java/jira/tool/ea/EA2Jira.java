package jira.tool.ea;

import org.apache.poi.xssf.usermodel.XSSFSheet;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;

public class EA2Jira {
    public static void process(XSSFSheet ea, XSSFWorkbook wb) {
        if (ea == null || wb == null) {
            return;
        }


    }
}
